
 Usage: `#[auto_assign(field = value,...)]`
 What it does:
 - it assembles several small internal macros that can inject the final state-transitioned struct into the function body at compile-time.
 - These internal macros are exposed at the #[type_state] root and can be implicitly invoked by the consumer.
 - The consumer can now create type state builders with dozens of fields without the repeated & annoying struct construction boilerpalte.
 - This is especially useful for complex state transition logic where a state has multiple dependencies.

 This macro is consumed by the `#[impl_state]` macro, and it basically guides `#[impl_state]` macro to:
 - construct the return structs of the methods generated by the `#[impl_state]` macro

 hence, it is empty, because it delegates its job to `#[impl_state]` macro
 the reason for that delegation is: `#[switch_to]` macro needs the below from the encapsulating `impl` block for the methods
 - name of the impl block (name of the struct)

 Details
 Instead of:
```rs
 #[require(RaceSet, B, C, D, E, F, G)]
 #[switch_to(RaceSet, LevelSet, SkillSlotsSet, D, E, F, G)]
 fn set_level(
     self,
     level_modifier: u8,
     init_skill_slots: u8
 ) -> PlayerBuilder {
     let level = match self.race {
         Some(Race::Orc) => level_modifier + 2, // Orc's have +2 level advantage
         Some(Race::Human) => level_modifier,   // humans are weak
         None => unreachable!("type safety ensures that `race` is initialized"),
     };

     PlayerBuilder {
         race:        self.race,
         level:       Some(level),
         skill_slots: Some(init_skill_slots),
         spell_slots: self.spell_slots,
         health:      self.health,
         strength:    self.name,
         agility:     self.agility,
         }
     }
```
 we can now do:

```rs
 #[auto_assign(level = level, skill_slots = init_skill_slots)]
 #[require(RaceSet, B, C, D, E, F, G)]
 #[switch_to(RaceSet, LevelSet, SkillSlotsSet, D, E, F, G)]
 fn set_level(
     self,
     level_modifier: u8,
     init_skill_slots: u8
 ) -> PlayerBuilder {
     let level = match self.race {
         Some(Race::Orc) => level_modifier + 2, // Orc's have +2 level advantage
         Some(Race::Human) => level_modifier,   // humans are weak
         None => unreachable!("type safety ensures that `race` is initialized"),
     };
 }

 

 Wiring up several setters is now not as dreadful anymore
 TODO: Refactoring, Docs, Tests, Review

```
---
- partial expansion snapshot using `cargo expand --ugly --test simple_example`. Struct is already expanded in set_level method
#### UX
```rs
#[type_state(states = (Initial, RaceSet, LevelSet, SkillSlotsSet, SpellSlotsSet), slots = (Initial, Initial, Initial))]
struct PlayerBuilder {
  race:        Option<Race>,
  level:       Option<u8>,
  skill_slots: Option<u8>,
  spell_slots: Option<u8>,
}

#[impl_state]
impl PlayerBuilder {
  #[require(Initial, Initial, Initial)] // require the default state for the constructor
  fn new() -> Self {
    PlayerBuilder {
      race:        None,
      level:       None,
      skill_slots: None,
      spell_slots: None,
    }
  }

  #[auto_assign(level = some_level_calc, race = race)]
  #[require(RaceSet, B, C)]
  #[switch_to(RaceSet, LevelSet, C)]
  fn set_level(
    self,
    level_modifier: u8,
    race: Race,
  ) -> PlayerBuilder {
    let some_level_calc = match self.race {
      Some(Race::Orc) => level_modifier + 2,
      Some(Race::Human) => level_modifier,
      None => unreachable!("type safety ensures that `race` is initialized"),
    };
  }
  ```

#### autogen step
```rs
#[allow(clippy :: type_complexity)]
struct PlayerBuilder<PlayerBuilderState1 = Initial, PlayerBuilderState2 =
    Initial, PlayerBuilderState3 = Initial> where
    PlayerBuilderState1: SealerPlayerBuilder,
    PlayerBuilderState2: SealerPlayerBuilder,
    PlayerBuilderState3: SealerPlayerBuilder {
    race: Option<Race>,
    level: Option<u8>,
    skill_slots: Option<u8>,
    spell_slots: Option<u8>,
    _state: (::core::marker::PhantomData<fn() -> PlayerBuilderState1>,
    ::core::marker::PhantomData<fn() -> PlayerBuilderState2>,
    ::core::marker::PhantomData<fn() -> PlayerBuilderState3>),
}
macro_rules! __state_shift_auto_assign_PlayerBuilder_race {
    ($s : expr, race = $val : expr, $ ($rest : tt) *) => { Some($val) };
    ($s : expr, race = $val : expr) => { Some($val) };
    ($s : expr, $other : ident = $val : expr, $ ($rest : tt) *) =>
    { __state_shift_auto_assign_PlayerBuilder_race! ($s, $ ($rest) *) };
    ($s : expr, $other : ident = $val : expr) =>
    { __state_shift_auto_assign_PlayerBuilder_race! ($s) }; ($s : expr) =>
    { $s.race };
}
macro_rules! __state_shift_auto_assign_PlayerBuilder_level {
    ($s : expr, level = $val : expr, $ ($rest : tt) *) => { Some($val) };
    ($s : expr, level = $val : expr) => { Some($val) };
    ($s : expr, $other : ident = $val : expr, $ ($rest : tt) *) =>
    { __state_shift_auto_assign_PlayerBuilder_level! ($s, $ ($rest) *) };
    ($s : expr, $other : ident = $val : expr) =>
    { __state_shift_auto_assign_PlayerBuilder_level! ($s) }; ($s : expr) =>
    { $s.level };
}
macro_rules! __state_shift_auto_assign_PlayerBuilder_skill_slots {
    ($s : expr, skill_slots = $val : expr, $ ($rest : tt) *) =>
    { Some($val) }; ($s : expr, skill_slots = $val : expr) => { Some($val) };
    ($s : expr, $other : ident = $val : expr, $ ($rest : tt) *) =>
    {
        __state_shift_auto_assign_PlayerBuilder_skill_slots! ($s, $ ($rest) *)
    }; ($s : expr, $other : ident = $val : expr) =>
    { __state_shift_auto_assign_PlayerBuilder_skill_slots! ($s) }; ($s : expr)
    => { $s.skill_slots };
}
macro_rules! __state_shift_auto_assign_PlayerBuilder_spell_slots {
    ($s : expr, spell_slots = $val : expr, $ ($rest : tt) *) =>
    { Some($val) }; ($s : expr, spell_slots = $val : expr) => { Some($val) };
    ($s : expr, $other : ident = $val : expr, $ ($rest : tt) *) =>
    {
        __state_shift_auto_assign_PlayerBuilder_spell_slots! ($s, $ ($rest) *)
    }; ($s : expr, $other : ident = $val : expr) =>
    { __state_shift_auto_assign_PlayerBuilder_spell_slots! ($s) }; ($s : expr)
    => { $s.spell_slots };
}
macro_rules! __state_shift_auto_assign_PlayerBuilder {
    ($s : expr, $state_expr : expr, $ ($pairs : tt) *) =>
    {
        PlayerBuilder
        {
            race : __state_shift_auto_assign_PlayerBuilder_race!
            ($s, $ ($pairs) *), level :
            __state_shift_auto_assign_PlayerBuilder_level! ($s, $ ($pairs) *),
            skill_slots : __state_shift_auto_assign_PlayerBuilder_skill_slots!
            ($s, $ ($pairs) *), spell_slots :
            __state_shift_auto_assign_PlayerBuilder_spell_slots!
            ($s, $ ($pairs) *), _state : $state_expr
        }
    };
}
impl<B, C>
    PlayerBuilder<

    RaceSet, B, C> where B: SealerPlayerBuilder, C: SealerPlayerBuilder {
    fn set_level(self, level_modifier: u8, race: Race)
        -> PlayerBuilder<RaceSet, LevelSet, C> {
        let some_level_calc =
            match self.race {
                Some(Race::Orc) => level_modifier + 2,
                Some(Race::Human) => level_modifier,
                None =>
                    {
                    ::core::panicking::panic_fmt(format_args!("internal error: entered unreachable code: {0}",
                            format_args!("type safety ensures that `race` is initialized")));
                }
            };
        let _state =
            (::core::marker::PhantomData, ::core::marker::PhantomData,
                ::core::marker::PhantomData);
        PlayerBuilder {
            race: Some(race),
            level: Some(some_level_calc),
            skill_slots: self.skill_slots,
            spell_slots: self.spell_slots,
            _state: _state,
        }
    }
}
```